# Simple inventory state persistence

This will persist data based on what's streamed into the topic to be looked up later. 

# TODO: Look into using Apache Flink as it could probably solve the problem perhaps?
